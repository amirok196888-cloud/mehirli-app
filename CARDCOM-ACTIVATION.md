# הפעלת סליקת מחירלי בקארדקום

התשתית מיועדת לתשלום חודשי חד־פעמי של 49 ₪ עבור 30 ימי שימוש במחירלי. היא אינה מחייבת את הכרטיס אוטומטית בחודש הבא, אינה שומרת פרטי כרטיס, ואינה משנה את זרימת BuyTest.

## מה לבקש מקארדקום

1. לאשר שימוש באותו מסוף עבור שני המוצרים והדומיינים: BuyTest ומחירלי.
2. לאשר API v11 מסוג Low Profile וכן WebHook.
3. למסור `TerminalNumber` ו־`ApiName` של המסוף המאושר.
4. לאשר הפקת מסמך אוטומטית ושליחתו במייל לאחר עסקה.
5. אם נדרש, לפתוח מחלקה נפרדת למחירלי ולמסור את מספר המחלקה.
6. לאשר בכתב שהשם/לוגו הניטרלי במסוף מתאים לשני המוצרים של אותו עוסק.

## הגדרות סודיות בצד השרת

יש להגדיר ב־Supabase Edge Functions בלבד:

- `CARDCOM_TERMINAL_NUMBER`
- `CARDCOM_API_NAME`
- `CARDCOM_MEHIRLI_DEPARTMENT_ID` — אופציונלי
- `MEHIRLI_PUBLIC_URL=https://amirok196888-cloud.github.io/mehirli-app/`
- `MEHIRLI_ALLOWED_ORIGINS=https://amirok196888-cloud.github.io`

אסור להכניס את הערכים האלה ל־`app.js`, ל־GitHub או לדפדפן.

## סדר ההפעלה

1. להחיל את `v38-cardcom-platform.sql`.
2. לפרוס את `mehirli-cardcom-checkout` עם אימות JWT פעיל.
3. לפרוס את `mehirli-cardcom-webhook` ללא אימות JWT של Supabase; הפונקציה עצמה מאמתת כל תשלום מול קארדקום בצד השרת.
4. להגדיר את הסודות.
5. לבצע עסקת בדיקה מאושרת על פי הוראות קארדקום ולוודא תשלום, הפעלת מנוי ומסמך.
6. רק לאחר הבדיקה לשנות את `platform_billing_settings.payment_mode` מ־`manual` ל־`cardcom`.

## ההפרדה החשבונאית

- מוצר: `MEHIRLI-MONTHLY`
- תיאור שורה: `מחירלי – מנוי חודשי ל-30 יום`
- מקור מעקב: `mehirli / app / monthly_subscription`
- סכום העסקה נקרא מהגדרת המחיר בבסיס הנתונים ברגע פתיחת התשלום.
- לכל עסקה נשמרים מספר עסקה ומספר/קישור מסמך, ללא פרטי כרטיס.

## התנהגות השירות

- תשלום מאומת מוסיף 30 ימים למנוי.
- לאחר תום המנוי יש לכל היותר 48 שעות חסד.
- אם אין תשלום נוסף, השירות מושהה והמידע נשמר.
- ניסיון כפול משתמש באותו דף תשלום לזמן קצר; WebHook חוזר אינו מפעיל את המנוי פעמיים.
- כשל בהפקת חשבונית אינו מבטל תשלום תקין, אך יוצר התראה למנהל לבדיקה במסוף.
